/**
 * Duplicate Detection — Section 11
 *
 * Before writes, reads Shopify identifiers and checks:
 * - SKU match
 * - Barcode match
 * - Optional handle/title heuristic
 *
 * V0: show duplicates, default = skip, never update existing.
 */

// TODO: Milestone C implementation
export {};
